# Работает или метод из объекта (если указан self) или из класса (если нет)
class Point:
    color = "red"
    circle = 2

    def set_coords(self):
        print(f"Вызов метода set_coords {self}")

    def set_coords_2(self, x, y):
        self.x = x
        self.y = y

    def get_coords(self):
        return (self.x, self.y)

pt = Point()
pt.set_coords()

# Или же вызвать метод у класса, но явно передать экземпляр класса
Point.set_coords(pt)

# Работа с локальными атрибутами КОНКРЕТНОГО экземпляра класса
pt.set_coords_2(1, 2)
print(pt.__dict__)

# Простой метод получения атрибутов КОНКРЕТНОГО экземпляра класса
print(pt.get_coords())

# Метод это тоже атрибут класса (только ведет не на данные а на функции), поэтому  можем получить доступ через getattr
f = getattr(pt, 'get_coords')
print(f())



# Задание: Работа с методами экземпляра и класса

# Создайте класс Rectangle с атрибутом класса color "green" 
# Добавьте метод set_dimensions, который принимает self, width и height и устанавливает эти значения как атрибуты экземпляра.
# Добавьте метод get_area, который возвращает площадь прямоугольника.

# Создайте экземпляр класса Rectangle с именем rect1.
# Вызовите метод set_dimensions через экземпляр rect1, передав значения width=10 и height=5.
# Вызовите метод get_area через экземпляр rect1 и выведите результат.
# Теперь вызовите метод set_dimensions через сам класс Rectangle, явно передав экземпляр rect1 и новые значения width=20 и height=10.
# Снова вызовите метод get_area и выведите результат.



# Используя getattr, получите метод get_area из экземпляра rect1 и вызовите его.

# Выведите содержимое __dict__ для rect1 и объясните, какие атрибуты были добавлены.



# Задание 2: Динамическое добавление и вызов методов

# Создайте класс Calculator с атрибутами класса name (по умолчанию "Simple Calculator").

# Добавьте метод add, который принимает self, a и b и возвращает их сумму.

# Создайте экземпляр класса Calculator с именем calc.

# Используя setattr, добавьте новый метод multiply в экземпляр calc. Этот метод должен принимать self, a и b и возвращать их произведение.

# Вызовите метод add через экземпляр calc, передав значения 5 и 10.

# Используя getattr, получите метод multiply из экземпляра calc и вызовите его, передав значения 4 и 6.

# Попробуйте вызвать метод multiply через сам класс Calculator. Объясните, почему это не работает.

# Используя hasattr, проверьте, существует ли метод multiply у экземпляра calc и у класса Calculator.

# Добавьте метод subtract в сам класс Calculator, который принимает self, a и b и возвращает разность a - b.

# Вызовите метод subtract через экземпляр calc и через класс Calculator, передав значения 20 и 7.

















# 1 Создать страницу index.html и разместить в  ней небольшой рекламный банер размером в 500px 
# 2 Банер (фон) сделать желтого цвета
# 3 Добавить картинку с рекламой (на выбор)
# 4 Заголовок 2-го уровня 
# 5 Описание рекламы в теге p (параграф) и ссылку с значением href равным # (заглушке) 